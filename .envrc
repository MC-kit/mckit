watch_file uv.lock
source ./.venv/bin/activate
export no_proxy="pypi.org,files.pythonhosted.org,github.com,api.github.com"  # to avoid SOCKS issue on python3.14
export MCKIT_PROJECT_VERSION="$(uv version --short)"
